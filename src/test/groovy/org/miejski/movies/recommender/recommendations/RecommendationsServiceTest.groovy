package org.miejski.movies.recommender.recommendations

import org.miejski.movies.recommender.infrastructure.Neo4jSpecification

class RecommendationsServiceTest extends Neo4jSpecification {


    def "hsdas"() {
        given:
        def a = ""

        when:
        b = a.bytes

        then:
        b == a
    }
}
